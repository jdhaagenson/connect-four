let winCount = 0
let rowIndex = 0
let columnIndex = 0

function winColumn() {
    columnIndex = currentColumn.index
    while (rowIndex < 7) {
        if (gridArray[columnIndex][rowIndex]==currentPlayer)
    }
}